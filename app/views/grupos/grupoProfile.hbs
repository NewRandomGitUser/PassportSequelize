
{{>_navbar}}
<div class="col-sm-9 col-md-9 col-lg-5 mx-auto mt-3">
<!-- <div class = "container mt-4"> -->
  <div class = "card">
    <div class = "card-body">
      <h2>Grupo {{grupo.dataValues.grupoNome}}</h2>
      <h5>Número de Integrantes: Teste</h5>
      <h5>Descrição: Teste</h5>
    </div>
  </div>

  <div class="col-sm- col-md- col-lg- mx-auto mt-5">
    <form name="criar_Post" method="POST" action = "/novaPostagem/{{grupo.dataValues.id}}">
      <textarea name = "conteudo" class="form-control"></textarea>
      <div class="d-flex flex-row-reverse">
        <button class="btn btn-outline-success p-2" type="submit">Criar postagem</button>
      </div>
    </form>
  </div>

  <div class = "card mt-4">
    <div class = "card-body">
      <h2>Nome, Nota: 9</h2>
      8 de junho às 13:37<br />
      <div class = "mt-4">
        REST defines a set of conventions to create HTTP services. The basic four methods can create CRUD (Create, Read, Update, Delete)
        • POST: Create a resource
        • GET: Read a resource
        • PUT: Update a resource
        • DELETE: Delete a resource
        In frontend side, you could do CRUD with form input as an example. The following is an example with POST. This case is pretty simple to run POST method only with HTML from front-end side.
      </div>
      <div class="d-flex flex-row-reverse">
        <div class = " card p-2 d-flex justify-content-center">Sinalizações: 12</div>
      </div>
    </div>
  </div>


  <div class = "card mt-4">
    <div class = "card-body">
      <h2>Nome, Nota: 9</h2>
      8 de junho às 13:37<br />
      <div class = "mt-4">
        REST defines a set of conventions to create HTTP services. The basic four methods can create CRUD (Create, Read, Update, Delete)
        • POST: Create a resource
        • GET: Read a resource
        • PUT: Update a resource
        • DELETE: Delete a resource
        In frontend side, you could do CRUD with form input as an example. The following is an example with POST. This case is pretty simple to run POST method only with HTML from front-end side.
      </div>
      <div class="d-flex flex-row-reverse">
        <div class = " card p-2 d-flex justify-content-center">Sinalizações: 12</div>
      </div>
    </div>
  </div>


  <div class = "card mt-4">
    <div class = "card-body">
      <h2>Nome, Nota: 9</h2>
      8 de junho às 13:37<br />
      <div class = "mt-4">
        REST defines a set of conventions to create HTTP services. The basic four methods can create CRUD (Create, Read, Update, Delete)
        • POST: Create a resource
        • GET: Read a resource
        • PUT: Update a resource
        • DELETE: Delete a resource
        In frontend side, you could do CRUD with form input as an example. The following is an example with POST. This case is pretty simple to run POST method only with HTML from front-end side.
      </div>
      <div class="d-flex flex-row-reverse">
        <div class = " card p-2 d-flex justify-content-center">Sinalizações: 12</div>
      </div>
    </div>
  </div>


<!-- col-sm-9 col-lg-3 mt-3 card p-2  -->

  <p>{{nome.firstname}} </p>
  <p>{{nome.lastname}} </p>
  <p>{{nome.email}} </p>


  {{#each postagem}}
    <p>
      {{dataValues.conteudo}}
    </p>
  {{else}}
  {{/each}}
</div>
